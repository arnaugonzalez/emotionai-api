global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "emotionai-api"
    scrape_interval: 10s
    metrics_path: "/metrics"
    static_configs:
      - targets: ["api:8000"]
        labels:
          environment: "development"

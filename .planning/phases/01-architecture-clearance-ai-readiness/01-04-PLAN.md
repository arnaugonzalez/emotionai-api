---
phase: 01-architecture-clearance-ai-readiness
plan: 04
type: execute
wave: 3
depends_on: ["01-01", "01-02", "01-03"]
files_modified:
  - docs/learning/ai_readiness_audit.md
  - .planning/phases/01-architecture-clearance-ai-readiness/01-VALIDATION.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "Audit document exists at docs/learning/ai_readiness_audit.md"
    - "Document contains all 5 required sections per CONTEXT.md"
    - "Personalization gap map covers every endpoint and service"
    - "M3 prerequisites are specific with file paths and task descriptions"
    - "VALIDATION.md frontmatter updated to mark phase validation complete"
  artifacts:
    - path: "docs/learning/ai_readiness_audit.md"
      provides: "Structured AI readiness assessment"
      contains: "## What Was Found"
      min_lines: 150
    - path: ".planning/phases/01-architecture-clearance-ai-readiness/01-VALIDATION.md"
      provides: "Updated validation sign-off"
      contains: "nyquist_compliant: true"
  key_links:
    - from: "docs/learning/ai_readiness_audit.md"
      to: "src/infrastructure/services/langchain_agent_service.py"
      via: "references personalization gaps"
      pattern: "langchain_agent_service"
---

<objective>
Write the AI readiness audit document synthesizing findings from the cleanup, repo implementations, and pgvector prep done in plans 01-03. Update VALIDATION.md sign-off.

Purpose: This is the final gate document before M3. It maps every personalization gap, embedding readiness status, and M3 prerequisite so that M3 planning can start from a concrete inventory rather than re-discovering the codebase.

Output: `docs/learning/ai_readiness_audit.md` — structured technical assessment. Updated VALIDATION.md frontmatter.
</objective>

<execution_context>
@/home/eager-eagle/.claude/get-shit-done/workflows/execute-plan.md
@/home/eager-eagle/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-architecture-clearance-ai-readiness/01-CONTEXT.md
@.planning/phases/01-architecture-clearance-ai-readiness/01-RESEARCH.md
@.planning/phases/01-architecture-clearance-ai-readiness/01-01-SUMMARY.md
@.planning/phases/01-architecture-clearance-ai-readiness/01-02-SUMMARY.md
@.planning/phases/01-architecture-clearance-ai-readiness/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write AI readiness audit document</name>
  <read_first>
    - src/infrastructure/services/langchain_agent_service.py
    - src/infrastructure/container.py
    - src/infrastructure/services/mock_similarity_search_service.py
    - src/infrastructure/services/mock_user_knowledge_service.py
    - src/infrastructure/database/models.py
    - src/presentation/api/routers/chat.py
    - src/presentation/api/routers/records.py
    - src/presentation/api/routers/breathing.py
    - src/presentation/api/routers/profile.py
    - src/domain/entities/user.py
    - src/domain/entities/agent_personality.py
    - src/domain/value_objects/user_profile.py
    - src/application/chat/use_cases/agent_chat_use_case.py
    - .planning/phases/01-architecture-clearance-ai-readiness/01-01-SUMMARY.md
    - .planning/phases/01-architecture-clearance-ai-readiness/01-02-SUMMARY.md
    - .planning/phases/01-architecture-clearance-ai-readiness/01-03-SUMMARY.md
  </read_first>
  <files>
    docs/learning/ai_readiness_audit.md
  </files>
  <action>
Create `docs/learning/ai_readiness_audit.md` following the project's learning doc convention (see ROADMAP.md learning documentation section). The document must be a structured technical assessment with these 5 required sections:

**Section 1: What Was Found**
Inventory of architecture issues discovered during the audit. Cover:
- Duplicate files found and their status (agent_chat_use_case already cleaned, tagging service shim, old openai_tagging_service)
- Auth dependency indirection in deps.py
- dependency-injector already removed
- 4 stub repositories with TODO methods
- Mock services in production container (MockSimilaritySearchService, MockUserKnowledgeService)
- pgvector not in docker-compose or requirements
- No embedding columns in schema
- Analytics repo has no ORM model (IAnalyticsRepository.save_agent_interaction has no persistence table)
- Crisis response protocol TODO at langchain_agent_service.py (find exact line by reading the file)
- OTEL instrumentation missing from feature-scoped openai_tagging_service

For each issue, state: file path, severity (P0/P1/P2), and whether it blocks M3.

**Section 2: What Was Fixed in This Phase**
Concrete list referencing actual changes made in plans 01-03. Read the SUMMARY files to get exact details. Expected items:
- Tagging service consolidation (root-level deleted, feature-scoped promoted to canonical with full interface, OTEL ported to feature-scoped openai_tagging_service, container import updated)
- Auth get_current_user_id consolidated (deps.py wrapper removed, 8 routers updated)
- 4 stub repositories implemented with real SQLAlchemy (emotional, breathing, analytics/log-only, events)
- Unit tests added for all 4 repos
- pgvector added to docker-compose (pgvector/pgvector:pg16 image)
- pgvector added to requirements.txt
- Alembic migration 005 created (vector extension + nullable embedding_vector on messages and emotional_records)
- ORM models updated with embedding_vector columns

**Section 3: M3 Prerequisites**
What MUST be addressed before M3 starts. For each item, provide: file path, specific task, estimated complexity. Items:
- Replace MockSimilaritySearchService with PgVectorSimilaritySearchService (file: src/infrastructure/services/mock_similarity_search_service.py, task: implement ISimilaritySearchService using pgvector cosine similarity, complexity: medium)
- Replace MockUserKnowledgeService with real implementation (file: src/infrastructure/services/mock_user_knowledge_service.py, task: aggregate user tags into knowledge profile, complexity: medium)
- Implement embedding pipeline (task: create Celery task that generates OpenAI embeddings and populates embedding_vector columns, complexity: high)
- Create AgentInteractionModel ORM table if persistent analytics needed (file: src/infrastructure/database/models.py, task: add new model + migration, complexity: low)
- Implement crisis response protocol (file: src/infrastructure/services/langchain_agent_service.py, line: [find exact line], task: handle crisis_detected flag with escalation, complexity: medium)
- Wire AgentPersonalityModel into LangChain agent context (currently not loaded into prompt)
- Wire UserProfileDataModel into LangChain agent context (personality_type, relaxation_tools not used)

**Section 4: Personalization Gap Map**
Table format per CONTEXT.md specifics:

| Endpoint / Service | Uses user_profile? | Uses agent_personality? | Gap Description |
|---|---|---|---|
| POST /v1/api/chat (LangChainAgentService) | Partial | No | Read the actual file to determine what context is passed to the agent prompt. Note which User entity fields are used vs which are ignored. AgentPersonalityModel is never queried. |
| POST /v1/api/chat (ISimilaritySearchService) | No (mock) | No | Returns empty — no semantic memory |
| POST /v1/api/chat (IUserKnowledgeService) | No (mock) | No | Returns None — no tag aggregation |
| GET /v1/api/emotional_records | No | No | Returns raw records, no personalization |
| POST /v1/api/emotional_records | No | No | Tags extracted but profile not enriched |
| GET /v1/api/breathing_* | No | No | No personalization |
| GET /v1/api/profile | Yes | Yes | Profile read works correctly |
| PUT /v1/api/profile | Yes | Yes | Profile update works correctly |

Read each router and service file to fill in accurate details. Do NOT copy the research table verbatim — verify by reading current code (which may have changed after plans 01-03).

**Section 5: Embedding Readiness**
Which data pipelines are ready for pgvector and which need work:
- messages table: has embedding_vector column (nullable), needs embedding generation pipeline (Celery task + OpenAI embedding API call)
- emotional_records table: has embedding_vector column (nullable), same pipeline need
- breathing_sessions table: does NOT have embedding column — assess whether breathing sessions benefit from semantic search (recommendation: unlikely, low-content data)
- user_profiles: no embedding column — assess whether profile text should be embedded (recommendation: yes, for similarity-based user clustering in M3+)
- conversations table: no embedding column — assess whether full conversations should be embedded (recommendation: individual messages are better targets)

For each pipeline, state: current status, what's needed, and estimated M3 effort.

**Document format requirements:**
- Follow the learning doc template from ROADMAP.md:
  ```
  # AI Readiness Audit — EmotionAI study guide
  ## What is it and why do we use it here
  ```
  Adapt the standard sections to match the 5 required sections above.
- Use actual file paths (not placeholder paths)
- Use actual line numbers where relevant
- Include code snippets showing current state where it clarifies the gap
- Minimum 150 lines
  </action>
  <verify>
    <automated>cd /home/eager-eagle/code/emotionai/emotionai-api && test -f docs/learning/ai_readiness_audit.md && wc -l docs/learning/ai_readiness_audit.md | awk '{if ($1 >= 150) print "LENGTH OK: "$1" lines"; else print "TOO SHORT: "$1" lines"; exit ($1 < 150)}' && grep -c "## " docs/learning/ai_readiness_audit.md | awk '{if ($1 >= 5) print "SECTIONS OK: "$1; else print "MISSING SECTIONS: "$1; exit ($1 < 5)}'</automated>
  </verify>
  <acceptance_criteria>
    - docs/learning/ai_readiness_audit.md exists
    - File is at least 150 lines long
    - File contains `## What Was Found` (or equivalent heading)
    - File contains `## What Was Fixed` (or equivalent heading)
    - File contains `## M3 Prerequisites` (or equivalent heading)
    - File contains `## Personalization Gap Map` (or equivalent heading)
    - File contains `## Embedding Readiness` (or equivalent heading)
    - File contains a markdown table with columns for endpoint, user_profile, agent_personality, gap
    - File contains `MockSimilaritySearchService` (documenting it as M3 prerequisite)
    - File contains `MockUserKnowledgeService` (documenting it as M3 prerequisite)
    - File contains `langchain_agent_service` (referencing the crisis protocol gap)
    - File contains `embedding_vector` (referencing the new columns)
  </acceptance_criteria>
  <done>AI readiness audit document written with all 5 sections, personalization gap map, embedding readiness assessment, and specific M3 prerequisites with file paths.</done>
</task>

<task type="auto">
  <name>Task 2: Update VALIDATION.md sign-off</name>
  <read_first>
    - .planning/phases/01-architecture-clearance-ai-readiness/01-VALIDATION.md
  </read_first>
  <files>
    .planning/phases/01-architecture-clearance-ai-readiness/01-VALIDATION.md
  </files>
  <action>
Update the VALIDATION.md frontmatter to reflect phase validation completion:

1. Set `nyquist_compliant: true` (replacing `false`)
2. Set `wave_0_complete: true` (replacing `false`)
3. In the Validation Sign-Off section, check all the checklist items:
   - `[x] All tasks have <automated> verify or Wave 0 dependencies`
   - `[x] Sampling continuity: no 3 consecutive tasks without automated verify`
   - `[x] Wave 0 covers all MISSING references`
   - `[x] No watch-mode flags`
   - `[x] Feedback latency < 30s`
   - `[x] nyquist_compliant: true set in frontmatter`
4. Set `**Approval:** complete` (replacing `pending`)
  </action>
  <verify>
    <automated>cd /home/eager-eagle/code/emotionai/emotionai-api && grep -q "nyquist_compliant: true" .planning/phases/01-architecture-clearance-ai-readiness/01-VALIDATION.md && grep -q "wave_0_complete: true" .planning/phases/01-architecture-clearance-ai-readiness/01-VALIDATION.md && echo "VALIDATION SIGN-OFF OK"</automated>
  </verify>
  <acceptance_criteria>
    - VALIDATION.md frontmatter has `nyquist_compliant: true`
    - VALIDATION.md frontmatter has `wave_0_complete: true`
    - Validation Sign-Off checklist items are all checked
    - Approval is `complete`
  </acceptance_criteria>
  <done>VALIDATION.md updated with phase validation sign-off.</done>
</task>

</tasks>

<verification>
- `docs/learning/ai_readiness_audit.md` exists with all 5 required sections
- Document references actual changes from plans 01-03 (verified by reading SUMMARY files)
- Personalization gap map covers all endpoints
- M3 prerequisites are specific and actionable
- VALIDATION.md frontmatter reflects completion
</verification>

<success_criteria>
- Audit document serves as a complete M3 planning input — no re-discovery needed
- All 5 sections present with file paths, specific tasks, and severity classifications
- Personalization gap map is accurate (verified against current codebase, not stale assumptions)
- VALIDATION.md sign-off complete
</success_criteria>

<output>
After completion, create `.planning/phases/01-architecture-clearance-ai-readiness/01-04-SUMMARY.md`
</output>

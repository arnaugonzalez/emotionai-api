---
phase: m2s1.1-demo-flow-hardening
plan: 01
subsystem: infra
tags: [bash, demo-flow, prometheus, metrics, observability]
requires:
  - phase: m2s1-prometheus-instrumentation
    provides: "FastAPI /metrics endpoint and pre-initialized EmotionAI metric families"
provides:
  - "Canonical demo runner entrypoint with section-aware orchestration"
  - "Shared bash registration and HTTP assertion helpers for additive observability steps"
  - "Strict metrics checks for /metrics availability, format, and required metric families"
affects: [m2s2-celery-redis-task-queue, m2s3-opentelemetry-tracing, demo-flow]
tech-stack:
  added: [bash]
  patterns: [step-registry, section-filtered-runner, shared-http-assertions]
key-files:
  created: [scripts/demo_flow_lib.sh, scripts/demo_steps/00_core.sh, scripts/demo_steps/10_metrics.sh]
  modified: [scripts/demo_flow.sh]
key-decisions:
  - "Kept scripts/demo_flow.sh as the canonical entry point and delegated execution logic to sourced step modules."
  - "Made metrics verification strict by checking HTTP 200, Prometheus text format, and required EmotionAI metric families."
  - "Locked a single register_step contract so future Celery and OTEL checks append modules instead of changing runner flow."
patterns-established:
  - "Step modules register metadata through register_step while the runner owns selection and ordering."
  - "HTTP-based demo assertions go through shared helpers that capture headers, body, stderr, and artifact paths."
requirements-completed: [M2S1.1-01, M2S1.1-02, M2S1.1-03, M2S1.1-04]
duration: 2m
completed: 2026-03-19
---

# Phase m2s1.1 Plan 01: Demo Flow Hardening Summary

**Section-aware bash demo runner with pluggable step modules and strict Prometheus metrics verification**

## Performance

- **Duration:** 2 min
- **Started:** 2026-03-19T13:28:01Z
- **Completed:** 2026-03-19T13:30:20Z
- **Tasks:** 3
- **Files modified:** 4

## Accomplishments
- Replaced the monolithic demo script with a small orchestration entry point, shared runner library, and sourced step modules.
- Added independent `metrics` coverage that fails on unreachable `/metrics`, invalid Prometheus exposition, or missing EmotionAI metric families.
- Enforced one additive registration contract so future observability slices can append steps without changing core runner logic.

## Task Commits

Each task was committed atomically:

1. **Task 1: Build modular demo runner skeleton with section-aware CLI** - `f748b5b` (feat)
2. **Task 2: Implement deterministic metrics checks as first-class steps** - `3193487` (feat)
3. **Task 3: Lock extension contract for additive future sections** - `c070dfa` (feat)

## Files Created/Modified
- `scripts/demo_flow.sh` - Canonical entrypoint with `--section`, `--list-steps`, `--base-url`, and runner orchestration.
- `scripts/demo_flow_lib.sh` - Shared registry, execution, summary, and HTTP/assertion helpers for demo steps.
- `scripts/demo_steps/00_core.sh` - Core health step on the canonical `/health/` route.
- `scripts/demo_steps/10_metrics.sh` - Metrics endpoint, format, and required-family checks under the `metrics` section.

## Decisions Made
- Kept the workflow in bash for this phase to preserve the existing operator command while still making the architecture modular.
- Used dynamically sourced step files plus a metadata registry instead of hardcoded flow branches in the entrypoint.
- Made `/health/` the core readiness path to match the existing router contract and avoid false failures on the non-canonical path.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Fixed Content-Type parsing in the shared HTTP helper**
- **Found during:** Task 2 (Implement deterministic metrics checks as first-class steps)
- **Issue:** The helper only recognized `Content-Type:` with exact casing, so a valid lowercase `content-type` header from Uvicorn caused a false metrics failure.
- **Fix:** Switched header parsing to lowercase matching and improved failure reason capture for step summaries.
- **Files modified:** `scripts/demo_flow_lib.sh`
- **Verification:** `bash scripts/demo_flow.sh --section metrics --base-url http://127.0.0.1:8000`
- **Committed in:** `3193487` (part of task commit)

---

**Total deviations:** 1 auto-fixed (1 bug)
**Impact on plan:** The auto-fix corrected a false negative in verification without expanding scope.

## Issues Encountered
- Parallel `git add` calls briefly created an index lock during Task 1 staging. No active git process remained, so staging was retried sequentially and completed cleanly.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- Future `celery`, `flower`, and `otel` checks can ship as additional `scripts/demo_steps/*.sh` modules without altering runner flow.
- The demo harness now provides one canonical command for targeted metrics verification and full observability walkthroughs.

## Self-Check: PASSED
- Verified summary file exists.
- Verified task commits `f748b5b`, `3193487`, and `c070dfa` exist in git history.

---
*Phase: m2s1.1-demo-flow-hardening*
*Completed: 2026-03-19*

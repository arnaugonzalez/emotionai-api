---
phase: m2s1.1-demo-flow-hardening
plan: 02
subsystem: infra
tags: [bash, demo-flow, observability, prometheus, shellcheck]
requires:
  - phase: m2s1.1-demo-flow-hardening
    provides: "Step-based demo runner with shared HTTP helpers and metrics checks"
provides:
  - "Run-scoped demo artifacts with per-step PASS/FAIL/SKIP diagnostics and remediation hints"
  - "Optional celery and otel step groups that skip cleanly when prerequisites are absent"
  - "Explicit required-vs-optional exit policy for section and full-run demo execution"
  - "Updated Prometheus learning guide for hardened demo runner usage and artifact troubleshooting"
affects: [m2s2-celery-redis-task-queue, m2s3-opentelemetry-tracing, demo-flow]
tech-stack:
  added: [shellcheck]
  patterns: [artifact-backed-step-results, optional-observability-sections, required-vs-optional-exit-policy]
key-files:
  created: [scripts/demo_steps/20_celery.sh, scripts/demo_steps/30_otel.sh]
  modified: [scripts/demo_flow.sh, scripts/demo_flow_lib.sh, docs/learning/prometheus_fastapi.md]
key-decisions:
  - "Stored each demo run under .tmp/demo_flow/<timestamp> so failure diagnostics always point to stable body, stderr, header, and log artifacts."
  - "Marked celery and otel checks as optional steps that SKIP when docker-compose services are missing or not running instead of polluting required flow status."
  - "Counted only required step failures toward process exit status while still surfacing optional failures and skips in the final summary."
patterns-established:
  - "Step functions write structured status, reason, remediation, and artifact files that the runner turns into consistent summaries."
  - "Future observability slices should add new placeholder steps as required=false first, then tighten them once their infrastructure is present."
requirements-completed: [M2S1.1-05, M2S1.1-06, M2S1.1-07, M2S1.1-08]
duration: 4m
completed: 2026-03-19
---

# Phase m2s1.1 Plan 02: Demo Flow Hardening Summary

**Artifact-backed demo diagnostics with optional Celery and OTEL sections, explicit exit semantics, and shellchecked runner modules**

## Performance

- **Duration:** 4 min
- **Started:** 2026-03-19T13:35:00Z
- **Completed:** 2026-03-19T13:39:21Z
- **Tasks:** 3
- **Files modified:** 5

## Accomplishments
- Added per-step result metadata so failures now report the exact condition, remediation hint, and artifact path under a timestamped `.tmp/demo_flow/...` directory.
- Registered additive `celery` and `otel` sections that run today and SKIP cleanly with prerequisite guidance when Flower, Jaeger, or OTLP infrastructure is absent.
- Locked the runner exit contract so required failures return non-zero while optional failures or skips remain visible but do not invalidate a successful required flow.

## Task Commits

Each task was committed atomically:

1. **Task 1: Implement actionable pass/fail diagnostics and run artifacts** - `718a4b5` (fix)
2. **Task 2: Add optional Celery and OTEL placeholder sections with clean skip behavior** - `c0be2b2` (feat)
3. **Task 3: Enforce required-vs-optional exit policy and shell quality baseline** - `a3fa30b` (chore)

## Files Created/Modified
- `scripts/demo_flow.sh` - Expanded CLI help to advertise the new section set.
- `scripts/demo_flow_lib.sh` - Added structured step result handling, artifact-backed diagnostics, compose/runtime helpers, and required-vs-optional exit tracking.
- `scripts/demo_steps/20_celery.sh` - Added optional Celery/Flower prerequisite and reachability checks with SKIP contracts.
- `scripts/demo_steps/30_otel.sh` - Added optional Jaeger and OTLP prerequisite/reachability checks with SKIP contracts.
- `docs/learning/prometheus_fastapi.md` - Documented `--section metrics`, `--section all`, `--list-steps`, and artifact-based failure troubleshooting.

## Decisions Made
- Kept the failure format in plain bash output rather than introducing a separate report format so demo results remain terminal-friendly and scriptable.
- Treated missing optional observability services as `SKIP`, not `FAIL`, because those stacks belong to later milestone slices and should not create false negatives today.
- Reused `docker-compose.yml` as the prerequisite contract for future sections so the runner can guide users toward the exact missing service definitions.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Fixed curl transport failure capture in the shared HTTP helper**
- **Found during:** Task 1 (Implement actionable pass/fail diagnostics and run artifacts)
- **Issue:** The helper initially collapsed connection failures into an HTTP `000` path without preserving the actual curl exit code, which weakened the new diagnostics.
- **Fix:** Updated curl execution handling so transport failures record the real curl exit code and point directly to the captured stderr artifact.
- **Files modified:** `scripts/demo_flow_lib.sh`
- **Verification:** `bash scripts/demo_flow.sh --section metrics --base-url http://127.0.0.1:9999`
- **Committed in:** `718a4b5` (part of task commit)

---

**Total deviations:** 1 auto-fixed (1 bug)
**Impact on plan:** The auto-fix tightened the planned diagnostics behavior without adding scope.

## Issues Encountered
- `shellcheck` flagged two unused step-context assignments in `scripts/demo_flow_lib.sh`; they were removed before the final shell quality verification run.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- Phase `m2s2` can replace the current Celery/Flower skips with real service checks without changing the runner contract.
- Phase `m2s3` can upgrade the OTEL placeholders into concrete Jaeger and OTLP assertions while keeping the same optional-step metadata model.

## Self-Check: PASSED
- Verified summary file exists at `.planning/phases/m2s1.1-demo-flow-hardening/m2s1.1-02-SUMMARY.md`.
- Verified task commits `718a4b5`, `c0be2b2`, and `a3fa30b` exist in git history.

---
*Phase: m2s1.1-demo-flow-hardening*
*Completed: 2026-03-19*
